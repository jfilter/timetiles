version: 2
updates:
  # npm dependencies (handles all pnpm workspaces)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
      time: "09:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 10
    versioning-strategy: "increase"
    labels:
      - "dependencies"
    # Cooldown: Wait 7 days for all releases to stabilize before creating PRs
    cooldown:
      default-days: 7
    # Group by risk level
    groups:
      # All minor and patch updates together (low-medium risk)
      minor-and-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    # Major updates get individual PRs (high risk, no grouping)

  # GitHub Actions updates
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      time: "09:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
    # Group all GitHub Actions updates together
    groups:
      github-actions:
        patterns:
          - "*"
