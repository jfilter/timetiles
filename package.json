{
  "name": "shadcn-ui-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "seed": "turbo seed",
    "seed:dev": "turbo seed:dev",
    "seed:test": "turbo seed:test",
    "seed:validate": "turbo seed:validate",
    "seed:truncate": "turbo seed:truncate",
    "seed:integration": "turbo seed:integration",
    "test": "turbo test",
    "docs:dev": "turbo docs:dev",
    "docs:build": "turbo docs:build",
    "docs:export": "turbo docs:export",
    "export": "turbo docs:export",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.13",
    "turbo": "^2.5.4",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=24"
  }
}
