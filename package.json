{
  "name": "timetiles",
  "description": "A data explorer for spatio-temporal data with custom metadata.",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint:fast",
    "lint:full": "turbo run lint",
    "lint:devops": "actionlint -shellcheck= && find . -name 'Dockerfile*' -type f | grep -v node_modules | xargs hadolint && find . \\( -name 'Makefile' -o -name '*.mk' \\) -type f | grep -v node_modules | xargs checkmake --config=.checkmake && find . -name '*.sh' -type f | grep -v node_modules | xargs shellcheck",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "test:coverage:merge": "tsx scripts/merge-coverage.ts",
    "format": "turbo run format",
    "typecheck": "turbo run typecheck:fast",
    "typecheck:full": "turbo run typecheck",
    "check": "turbo run lint:fast typecheck:fast",
    "check:full": "turbo run lint typecheck",
    "setup:git": "./scripts/setup-git.sh",
    "sonarcloud:fetch": "tsx scripts/fetch-sonarcloud-issues.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "@timetiles/eslint-config": "workspace:*",
    "@timetiles/typescript-config": "workspace:*",
    "@typescript/native-preview": "7.0.0-dev.20260129.1",
    "chalk": "^5.6.2",
    "glob": "^13.0.0",
    "husky": "^9.1.7",
    "nyc": "^17.1.0",
    "oxlint": "^1.42.0",
    "oxlint-tsgolint": "^0.11.3",
    "prettier": "catalog:",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "tsx": "catalog:",
    "turbo": "^2.8.0",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "overrides": {
      "pino": "9.7.0",
      "@headlessui/react": "^2.2.6",
      "@theguild/remark-mermaid": "^0.0.7",
      "next-mdx-remote": "^5.0.0",
      "form-data": "^4.0.4",
      "cross-spawn": "^7.0.6",
      "dompurify": "^3.2.3",
      "esbuild": "^0.25.0"
    },
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ],
    "peerDependencyRules": {
      "ignoreMissing": [
        "monaco-editor",
        "yjs",
        "puppeteer"
      ],
      "allowedVersions": {
        "@theguild/remark-mermaid>react": "^19",
        "@typescript-eslint/utils>eslint": "^9"
      }
    }
  }
}
