# `@timetiles/typescript-config`

Shared TypeScript configuration for the workspace.

This package contains only JSON configuration and documentation, so it needs formatting but no JavaScript lint task. The consuming packages validate these configurations through their typechecks.
