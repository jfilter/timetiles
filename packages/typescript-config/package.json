{
  "name": "@timetiles/typescript-config",
  "version": "0.0.0",
  "private": true,
  "license": "PROPRIETARY",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "oxlint --config ../../.oxlintrc.json . && pnpm -w lint:eslint packages/typescript-config --cache --cache-location packages/typescript-config/.eslintcache",
    "format": "oxfmt --write ."
  },
  "devDependencies": {
    "eslint": "catalog:"
  }
}
