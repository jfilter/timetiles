{
  "name": "@timetiles/typescript-config",
  "version": "0.0.0",
  "private": true,
  "license": "PROPRIETARY",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "mkdir -p .lint-results && eslint . --ext .ts,.tsx --cache --format json --output-file .lint-results/latest.json; eslint . --ext .ts,.tsx --cache",
    "format": "eslint . --ext .ts,.tsx,.js,.jsx --cache --fix --quiet && prettier --write . --log-level error"
  },
  "devDependencies": {
    "eslint": "catalog:",
    "prettier": "catalog:"
  }
}
