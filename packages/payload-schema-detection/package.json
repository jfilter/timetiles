{
  "name": "@timetiles/payload-schema-detection",
  "version": "0.0.1",
  "private": true,
  "description": "Payload CMS plugin for schema detection with language-aware field mapping",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./utilities": "./src/utilities/index.ts",
    "./detectors": "./src/detectors/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "lint": "mkdir -p .lint-results && eslint . --cache --format json --output-file .lint-results/latest.json; eslint . --cache",
    "format": "eslint . --fix --quiet --cache && oxfmt --write .",
    "typecheck": "tsx ../../scripts/typecheck-with-json.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "franc": "catalog:"
  },
  "devDependencies": {
    "@timetiles/eslint-config": "workspace:*",
    "@timetiles/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "payload": "^3.73.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "payload": "^3.0.0"
  }
}
