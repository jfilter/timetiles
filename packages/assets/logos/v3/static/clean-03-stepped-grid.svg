<svg width="120" height="120" viewBox="0 0 120 120" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="clean3-blue" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#0ea5e9"/>
      <stop offset="100%" style="stop-color:#0284c7"/>
    </linearGradient>
  </defs>

  <g transform="translate(60, 60)">
    <!-- Stepped tiles creating temporal progression -->

    <!-- Left column (past) -->
    <rect x="-38" y="-20" width="16" height="16" fill="url(#clean3-blue)" opacity="0.5" rx="2"/>
    <rect x="-38" y="4" width="16" height="16" fill="url(#clean3-blue)" opacity="0.5" rx="2"/>

    <!-- Middle column (transitional) -->
    <rect x="-18" y="-28" width="18" height="18" fill="url(#clean3-blue)" opacity="0.7" rx="2"/>
    <rect x="-18" y="-6" width="18" height="18" fill="url(#clean3-blue)" opacity="0.7" rx="2"/>
    <rect x="-18" y="16" width="18" height="18" fill="url(#clean3-blue)" opacity="0.7" rx="2"/>

    <!-- Right column (present) -->
    <rect x="4" y="-36" width="20" height="20" fill="url(#clean3-blue)" opacity="0.9" rx="2"/>
    <rect x="4" y="-12" width="20" height="20" fill="url(#clean3-blue)" opacity="0.9" rx="2"/>
    <rect x="4" y="12" width="20" height="20" fill="url(#clean3-blue)" opacity="0.9" rx="2"/>

    <!-- Far right accent (future) -->
    <rect x="28" y="-24" width="12" height="12" fill="#0ea5e9" opacity="1" rx="2"/>
  </g>
</svg>
