/**
 * PostCSS configuration file.
 *
 * Re-exports the PostCSS configuration from the shared UI package
 * for consistent styling across the monorepo.
 *
 * @module
 */
export { config as default } from "@workspace/ui/postcss.config";
