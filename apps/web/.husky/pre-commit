#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Generate Payload types before commit
cd apps/web && pnpm payload:generate

# Add the generated types to the commit if they changed
git add payload-types.ts