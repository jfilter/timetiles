{
  "name": "docs",
  "version": "0.0.1",
  "private": true,
  "description": "The documentation site for the TimeTiles project.",
  "type": "module",
  "scripts": {
    "dev": "pnpm docs:api && next dev --port 3001",
    "build": "pnpm docs:api && next build --webpack",
    "docs:api": "cd ../web && pnpm exec typedoc && tsx ../docs/scripts/clean-typedoc-output.ts && tsx ../docs/scripts/generate-api-meta.ts",
    "docs:api:watch": "nodemon --watch ../web/lib --ext ts --exec \"pnpm docs:api\"",
    "start": "next start --port 3001",
    "lint": "oxlint --config ../../.oxlintrc.json . && mkdir -p .lint-results && eslint app content scripts --cache --format json --output-file .lint-results/latest.json; eslint app content scripts --cache && SKIP_EXTERNAL=1 tsx scripts/check-links.ts",
    "lint:all": "eslint app content scripts --cache && tsx scripts/check-links.ts",
    "typecheck": "tsx ../../scripts/typecheck-with-json.ts",
    "typecheck:fast": "tsgo --noEmit",
    "format": "eslint app content scripts --cache --fix --quiet && oxfmt --write ."
  },
  "dependencies": {
    "@timetiles/assets": "workspace:*",
    "@timetiles/ui": "workspace:*",
    "next": "catalog:",
    "nextra": "^4.6.1",
    "nextra-theme-docs": "^4.6.1",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@svgr/webpack": "catalog:",
    "@timetiles/eslint-config": "workspace:*",
    "@timetiles/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "eslint": "catalog:",
    "glob": "^13.0.6",
    "nodemon": "^3.1.14",
    "tsx": "catalog:",
    "typedoc": "^0.28.17",
    "typedoc-plugin-markdown": "^4.10.0",
    "typescript": "catalog:"
  }
}
