# Data Flow

How data enters, is processed, stored, queried, and displayed across TimeTiles.

## System Overview

End-to-end view from data entry through processing, storage, and display.

```mermaid
%%{init: {'theme': 'neutral', 'flowchart': {'nodeSpacing': 30, 'rankSpacing': 40}}}%%
flowchart TB
    subgraph INPUT["Entry Points"]
        CSV["File Upload"]
        URL["URL Fetch"]
        SCHED["Scheduled Import"]
        HOOK["Webhook"]
        SCRAPER["Scraper"]
    end

    subgraph PREVIEW["Preview & Configure"]
        DETECT["Detect format & sheets"]
        CONFIGURE["Map fields & create dataset"]
    end

    subgraph WORKFLOWS["Workflows"]
        WF_MANUAL["manual-ingest"]
        WF_SCHED["scheduled-ingest"]
        WF_SCRAPER["scraper-ingest"]
        WF_REVIEW["ingest-process"]
    end

    subgraph PIPELINE["Per-Sheet Pipeline"]
        T1["Analyze Duplicates"]
        T2["Detect Schema"]
        T3["Validate Schema"]
        T4["Create Schema Version"]
        T5["Geocode Batch"]
        T6["Create Events"]
    end

    subgraph STORAGE["PostgreSQL + PostGIS"]
        EVENTS[("events")]
        DATASETS[("datasets")]
        SCHEMAS[("dataset-schemas")]
        LOC_CACHE[("location-cache")]
    end

    subgraph OUTPUT["Display"]
        MAP["Map + Clusters"]
        TIMELINE["Histogram"]
        LIST["Event List"]
        STATS["Statistics"]
    end

    CSV & URL --> DETECT --> CONFIGURE
    CONFIGURE --> WF_MANUAL
    SCHED --> WF_SCHED
    HOOK --> WF_SCHED & WF_SCRAPER
    SCRAPER --> WF_SCRAPER

    WF_MANUAL & WF_SCHED & WF_SCRAPER --> T1
    T1 --> T2 --> T3 --> T4 --> T5 --> T6
    T3 -.->|"needs review"| WF_REVIEW --> T4

    T4 --> SCHEMAS
    T5 --> LOC_CACHE
    T6 --> EVENTS
    CONFIGURE --> DATASETS

    EVENTS --> MAP & TIMELINE & LIST & STATS
```

**Entry points:** Files (CSV, Excel, ODS) are uploaded directly. URLs and scrapers go through scheduled or manual triggers. Webhooks can trigger both scheduled imports and scraper runs.

**Workflows:** Four Payload Workflows on the `ingest` queue handle the different entry paths. All converge into the same 6-task per-sheet pipeline.

**Storage:** Events are stored with PostGIS Point geometry, JSONB original data, and denormalized access fields for zero-query permission checks.

---

## Import Pipeline Detail

The 6-task sheet processing pipeline. For stage-by-stage documentation, see [Processing Stages](/development/architecture/data-processing-pipeline/stages).

### File Reading

```mermaid
%%{init: {'theme': 'neutral'}}%%
flowchart LR
    FILE["Uploaded File"] --> READER["streamBatchesFromFile"]
    READER -->|".csv"| PAPA["Papa Parse\n(streaming, ~3MB/batch)"]
    READER -->|".xlsx / .ods"| EXCEL["SheetJS\n(sheet → CSV)"] --> PAPA
    URL_SRC["URL Source"] --> JSON_CSV["JSON → CSV\nconversion"] --> PAPA
    PAPA --> TRANSFORMS["Apply Transforms\nrename, date-parse,\nstring-op, concat, split"]
```

### Processing Pipeline

```mermaid
%%{init: {'theme': 'neutral', 'flowchart': {'nodeSpacing': 20, 'rankSpacing': 35}}}%%
flowchart TD
    subgraph S1["1. Analyze Duplicates"]
        GEN_ID["Generate unique ID"]
        INT_DUP["Find internal duplicates"]
        EXT_DUP["Find external duplicates"]
        SKIP["Build skip-row list"]
    end

    subgraph S2["2. Detect Schema"]
        INFER["Type inference"]
        GEO["Detect geo fields"]
        FIELD_STATS["Field statistics"]
    end

    subgraph S3["3. Validate Schema"]
        COMPARE["Compare with\nprevious version"]
        QUOTA["Check quota"]
        REVIEW{"Needs\nreview?"}
    end

    S4["4. Create Schema Version"]

    subgraph S5["5. Geocode Batch"]
        EXTRACT["Extract unique locations"]
        CACHE["Check LocationCache"]
        PROVIDERS["Nominatim / Google\n/ OpenCage"]
    end

    subgraph S6["6. Create Events"]
        BUILD["Build event objects"]
        DEDUP["Set dedup ID"]
        INSERT["Bulk INSERT\nON CONFLICT DO NOTHING"]
    end

    DONE["COMPLETED"]
    WAIT["NEEDS_REVIEW"]
    ERR["FAILED"]

    GEN_ID --> INT_DUP --> EXT_DUP --> SKIP
    SKIP --> INFER --> GEO --> FIELD_STATS
    FIELD_STATS --> COMPARE --> QUOTA --> REVIEW
    REVIEW -->|"Yes"| WAIT -.->|"User approves"| S4
    REVIEW -->|"No"| S4
    S4 --> EXTRACT --> CACHE --> PROVIDERS
    PROVIDERS --> BUILD --> DEDUP --> INSERT --> DONE

    S1 -.->|"Error"| ERR
    S2 -.->|"Error"| ERR
    S5 -.->|"Error"| ERR
    S6 -.->|"Error"| ERR
```

**Deduplication strategies:** `"external-id"` (field from source data), `"computed-hash"` (hash of title + date + location), `"content-hash"` (hash of entire row), `"hybrid"` (try external ID, fall back to computed hash).

**Review gate:** The pipeline pauses per-sheet (not per-file) when schema changes are breaking or when it's the first import. The `ingest-process` workflow resumes after user approval.

---

## Query & Display Flow

How data flows from PostgreSQL through the filter system to the frontend.

```mermaid
%%{init: {'theme': 'neutral', 'flowchart': {'nodeSpacing': 25, 'rankSpacing': 40}}}%%
flowchart LR
    subgraph UI["Browser"]
        URL_STATE["URL State\n(nuqs)"]
        FILTERS["useFilters"]
    end

    subgraph HOOKS["React Query"]
        CLUSTERS["fetchMapClusters"]
        BOUNDS["fetchEventBounds"]
        HIST["fetchHistogram"]
        AGG["fetchAggregateStats"]
        EVENTS_LIST["fetchEvents"]
    end

    subgraph API["/api/v1/events/*"]
        GEO["/geo"]
        BOUNDS_R["/bounds"]
        TEMPORAL["/temporal"]
        STATS_R["/stats"]
        LIST_R["/"]
    end

    subgraph ACCESS["Access Control"]
        RESOLVE["resolveEventQueryContext"]
        CANONICAL["buildCanonicalFilters"]
    end

    subgraph DB["PostGIS"]
        CLUSTER_FN["cluster_events()"]
        HIST_FN["calculate_event_histogram()"]
        SQL["SQL queries"]
    end

    subgraph RENDER["Display"]
        MAP["Map + Clusters"]
        CHART["Histogram"]
        TABLE["Event List"]
        BAR["Statistics"]
    end

    URL_STATE --> FILTERS --> HOOKS
    CLUSTERS --> GEO
    BOUNDS --> BOUNDS_R
    HIST --> TEMPORAL
    AGG --> STATS_R
    EVENTS_LIST --> LIST_R

    API --> RESOLVE --> CANONICAL

    CANONICAL --> CLUSTER_FN --> MAP
    CANONICAL --> HIST_FN --> CHART
    CANONICAL --> SQL --> TABLE & BAR
```

**Access control:** `resolveEventQueryContext` determines which catalogs the user can see (public + own for authenticated users). `buildCanonicalFilters` normalizes URL parameters into validated SQL conditions with field whitelisting against SQL injection.

**PostGIS functions:** `cluster_events()` does server-side spatial clustering by zoom level and bounds. `calculate_event_histogram()` computes time-bucketed event counts with automatic bucket sizing.

---

## Background Systems

Job queues, scheduling, scraper execution, and maintenance.

```mermaid
%%{init: {'theme': 'neutral', 'flowchart': {'nodeSpacing': 20, 'rankSpacing': 35}}}%%
flowchart TB
    subgraph TRIGGERS["Triggers"]
        CRON["schedule-manager\n(every minute)"]
        WEBHOOK["Webhook POST"]
        MANUAL["Manual API call"]
        UPLOAD["File upload hook"]
        APPROVE["Review approval hook"]
    end

    subgraph QUEUES["Queues"]
        Q_INGEST["ingest"]
        Q_DEFAULT["default"]
        Q_MAINT["maintenance"]
    end

    subgraph WORK["Workflows"]
        WF1["manual-ingest"]
        WF2["scheduled-ingest"]
        WF3["scraper-ingest"]
        WF4["ingest-process"]
    end

    subgraph SCRAPER["Scraper Execution"]
        SYNC["Repo sync"]
        RUNNER["TimeScrape Runner"]
        PODMAN["Podman container\n(sandboxed)"]
    end

    subgraph MAINT["Maintenance Jobs"]
        M1["quota-reset — daily midnight"]
        M2["cache-cleanup — every 6h"]
        M3["schema-maintenance — daily 3 AM"]
        M4["audit-log-ip-cleanup — daily 4 AM"]
        M5["account-deletion — daily 2 AM"]
        M6["data-export-cleanup — hourly"]
        M7["stuck ingests/scrapers — hourly"]
    end

    CRON --> Q_INGEST & Q_DEFAULT
    WEBHOOK --> Q_INGEST
    MANUAL --> Q_INGEST
    UPLOAD --> Q_INGEST
    APPROVE --> Q_INGEST

    Q_INGEST --> WF1 & WF2 & WF3 & WF4
    Q_MAINT --> MAINT

    WF3 --> SYNC --> RUNNER --> PODMAN
```

**Three queues:** `ingest` for import workflows (dedicated Docker worker in production), `default` for trigger jobs like `schedule-manager`, `maintenance` for periodic system tasks.

**Scraper isolation:** Scrapers run in rootless Podman containers with read-only filesystem, no network access, CPU/memory limits, and user namespace isolation. The TimeScrape runner (Hono API server) orchestrates execution.

---

## Data Organization & Access Control

Multi-tenant data model with denormalized access fields.

```mermaid
%%{init: {'theme': 'neutral'}}%%
flowchart TD
    SITE["Site\n(domain mapping)"] -->|"has many"| VIEW["View\n(data scope)"]
    VIEW -->|"scopes to"| CATALOG["Catalog\n(ownership, visibility)"]
    CATALOG -->|"has many"| DATASET["Dataset\n(schema, transforms)"]
    DATASET -->|"has many"| SCHEMA_V["DatasetSchema\n(versioned fields)"]
    DATASET -->|"has many"| EVENT["Event\n(PostGIS Point, JSONB)"]

    EVENT --- D1["catalogOwnerId"]
    EVENT --- D2["datasetIsPublic"]

    subgraph ACCESS["Access Levels"]
        ANON["Anonymous → public catalogs"]
        AUTH["Authenticated → public + own"]
        EDITOR["Editor → all catalogs"]
        ADMIN["Admin → everything"]
    end

    ACCESS -->|"getAllAccessibleCatalogIds"| CATALOG
```

**Denormalized fields:** `catalogOwnerId` and `datasetIsPublic` are copied onto every event row. This enables zero-join access control — SQL queries filter directly on these fields instead of joining through catalog/dataset tables.

**Site & View resolution:** Cached with 5-minute TTL. Sites map domains to configurations. Views define which catalogs/datasets are visible (scopes: `all`, `catalogs`, or `datasets`).
