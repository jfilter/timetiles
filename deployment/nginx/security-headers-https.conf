# Security headers for HTTPS server blocks (adds HSTS on top of base headers)
# Include this in HTTPS server blocks: include /etc/nginx/security-headers-https.conf;

include /etc/nginx/security-headers.conf;
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
