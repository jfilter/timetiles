#!/usr/bin/env sh

# Run linting and TypeScript checking sequentially to ensure proper exit codes
# Temporarily disabled ESLint to focus on TypeScript errors
# pnpm lint && pnpm typecheck

# This for Google's Jules
if ! command -v pnpm >/dev/null 2>&1; then
  exit 0
fi

pnpm typecheck
