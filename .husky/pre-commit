#!/usr/bin/env sh

# Regenerate Payload types if migrations or collections changed
./scripts/pre-commit-payload-types.sh

# Run linting and TypeScript checking sequentially to ensure proper exit codes
pnpm lint && pnpm typecheck
