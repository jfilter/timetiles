#!/usr/bin/env sh

# Regenerate Payload types if migrations or collections changed
./scripts/pre-commit-payload-types.sh

# Run fast checks first (oxlint + tsgo), then full checks (eslint + tsc) only if fast passes.
pnpm lint && pnpm typecheck && pnpm lint:full && pnpm typecheck:full
