# Hadolint configuration - minimal ignores only
# https://github.com/hadolint/hadolint

ignored:
  # DL3008: Pin versions in apt-get install
  # DL3018: Pin versions in apk add
  # Distro packages should track the base image version, not be pinned
  - DL3008
  - DL3018
